name: CD

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

env:
  AWS_REGION: ap-northeast-2
  S3_BUCKET: soupspoon.kr

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy to S3
        run: aws s3 sync dist/ s3://${{ env.S3_BUCKET }} --delete

      - name: Notify Discord (success)
        if: success()
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            {
              "embeds": [{
                "title": "🚀 배포 성공",
                "description": "프론트엔드가 성공적으로 배포되었습니다.",
                "color": 5814783,
                "fields": [
                  {
                    "name": "Repository",
                    "value": "`${{ github.repository }}`",
                    "inline": true
                  },
                  {
                    "name": "Source Branch (PR)",
                    "value": "`${{ github.event.pull_request.head.ref || github.ref_name }}`",
                    "inline": true
                  },
                  {
                    "name": "Target Branch (PR)",
                    "value": "`${{ github.event.pull_request.base.ref || github.ref_name }}`",
                    "inline": true
                  },
                  {
                    "name": "Commit",
                    "value": "`${{ steps.commit.outputs.commit_hash }}`\n${{ steps.commit.outputs.message }}"
                  },
                  {
                    "name": "Author",
                    "value": "`${{ steps.commit.outputs.author }}`",
                    "inline": true
                  }
                ],
                "timestamp": "${{ github.run_started_at }}"
              }]
            }

      - name: Notify Discord (failure)
        if: failure()
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            {
              "embeds": [{
                "title": "❌ 배포 실패",
                "description": "배포 중 오류가 발생했습니다.",
                "color": 15158332,
                "fields": [
                  {
                    "name": "Repository",
                    "value": "`${{ github.repository }}`",
                    "inline": true
                  },
                  {
                    "name": "Source Branch (PR)",
                    "value": "`${{ github.event.pull_request.head.ref || github.ref_name }}`",
                    "inline": true
                  },
                  {
                    "name": "Target Branch (PR)",
                    "value": "`${{ github.event.pull_request.base.ref || github.ref_name }}`",
                    "inline": true
                  },
                  {
                    "name": "Commit",
                    "value": "`${{ steps.commit.outputs.commit_hash }}`\n${{ steps.commit.outputs.message }}"
                  },
                  {
                    "name": "Author",
                    "value": "`${{ steps.commit.outputs.author }}`",
                    "inline": true
                  }
                ],
                "timestamp": "${{ github.run_started_at }}"
              }]
            }
